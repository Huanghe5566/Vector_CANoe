---
description: CAPL/CANoe 12 编写与编译约束（T19C 工程，含 IPClient/FCR_GW 已验证做法）
globs: "**/*.{can,cin}"
alwaysApply: false
---

# CAPL / CANoe 12（奇瑞 T19C 工程）

编辑 `Code/*.can`、`Code/MainTest/*.can`、`Include/*.cin` 时遵守以下约束，避免反复出现编译 1002/1066/1140/2038。

## 文件编码

- CAPL 源文件头使用 **`/*@!Encoding:936*/`**（中文 Windows + CANoe 本地编码）。
- 同一编译单元内 **`#include` 的 .can/.cin 编码声明须一致**（如 `IPClient.can` 与 `IPCommon.can` 均为 936）。
- 注释与字符串：**优先纯 ASCII**；若含中文，全文件统一 GBK/936，勿与 UTF-8（65001）混编，否则 `writelineex`/`snprintf` 报 **2038**。
- 工程级 Markdown/DBC 仍可按仓库 UTF-8 规则；**仅 CAPL 走 936**。

## 禁止使用的 C 库函数（会 1156 unknown function）

- 不要用：`atof`、`atoi`、`atol`、`sscanf`、`strtod`。
- 十进制字符串转浮点：用工程内 **`ParseDecimalStringToFloat()`**（`IPClient.can`）或自写逐字符解析（`0-9`、可选 `.`、可选 `+/-`）。

## 事件处理程序（on …）语法

- **`on sysvar_update` / `on timer` / `on start` 等事件中禁止 `return`** → 用 `if / else`（否则 **1002 parse error**）。
- **不要用 `on signal_update`**（本环境不支持）。
- **不要用 `on signal sdadad::报文::信号`**（**1066 Unknown signal 'sdadad'**）；数据库名不是 signal 限定符。
- **不要在 signal 回调里用 `this` / `this.phys`**（**1140 'this' not allowed**）。

## 双 DBC（sdadad / sdada）歧义

- 工程同时加载 RLCR（`sdadad`）与 RRCR（`sdada`）库，报文名可能重复。
- 读写信号用：**`$CAN1::报文名::信号名.phys`**（与面板 GW 节点一致，走 RLCR 侧）。
- 勿在 `on signal` 里写库前缀消警告；消歧义靠 `$CAN1::` 赋值，不靠 `on signal sdadad::...`。

## GW 系统变量 ↔ CAN 信号（FCR_GW.can 模式）

需求：`GW::VehicleSpeed/SteeringAngle/YawRate` 变化时驱动信号；仅改面板信号时以信号为准。

| 系统变量 | CAN 信号 |
|----------|----------|
| `GW::VehicleSpeed` | `ABS_ESP_1::ABS_ESP_1_VehicleSpeedVSOSig` |
| `GW::SteeringAngle` | `SAM_1_G::SteeringAngle` |
| `GW::YawRate` | `YAS_1::YawRate` |

实现要点（不要用 `on signal`）：

1. `on sysvar_update` → `PushGw*ToSignal()`，置 `gGw*FromSysvar = 1`，写 `$CAN1::...phys`。
2. **`msTimer` 周期 50ms** → `SyncGwSysvarsFromPanelSignals()`：标志为 1 则清零；否则若 `信号.phys != @sysvar` 则回写 sysvar。
3. `on start`：`setTimerCyclic` + `ApplyGwSysvarToSignals()`；MainTest 在 `preStart` 的 `set_value_to_valid()` 之后仍可在 `on start` 再应用 sysvar。

## IPClient / UDP

- UDP 载荷写入 `UDP::UdpData` 后，用 **`ParseDecimalStringToFloat`** 赋 **`@sysvar::GW::VehicleSpeed`**（勿用 atof/sscanf）。

## 修改后检查

- 编译目标节点 CAPL（如 `FCR_GW.can`、`IPClient.can`），目标 **0 errors**。
- 若仅 warnings 2020（报文歧义），检查是否误用未限定的 `on signal 报文::信号`；应改为定时器同步或 `$CAN1::` 赋值。

## 参考文件

- `Code/IPClient.can`、`Code/IPCommon.can` — UDP + 编码 936
- `Code/FCR_GW.can`、`Code/MainTest/FCR_GW.can` — GW 同步 + `applILTxPending`
